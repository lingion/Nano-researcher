local-policy-agent 可移植性 / Zero-Config Startup 收口汇报
生成日期：2026-06-05
项目路径：/Users/lingion/repo-downloads/local-policy-agent

一、最终状态

local-policy-agent 已完成生产收口与另一台电脑可移植性审计。
当前仓库已经初始化为 git 仓库，并完成两次提交：

1. ca64766 chore: lock policy agent production baseline
   - 锁定生产 baseline
   - 归档 Heavy Prompt 实测基准
   - 纳入 README、fixtures、prompt-freeze 文档、核心源码与测试

2. e37851c chore: add portable startup checks
   - 补齐 zero-config startup 元数据
   - 新增 .nvmrc 与 .env.example
   - 新增 offline Golden Fixture 回归测试
   - 清理源码中的本机绝对路径依赖
   - 修正 package start 脚本

当前 git 状态为 clean：

  ## main

二、生产 baseline 收口内容

1. Git 仓库初始化

仓库路径：

  /Users/lingion/repo-downloads/local-policy-agent

当前分支：

  main

仓库已经从非 git 目录转为正式 git 仓库，并完成初始生产 baseline 提交。

2. NanoClaw gateway hard-lock

已确认 nanoclaw runtime 支持显式 provider 锁定：

  NANOCLAW_LLM_PROVIDER=openai

README 已明确说明：OpenAI-compatible gateway 路径必须显式设置该变量，不应依赖自动 provider inference。

3. Golden Fixture 归档

已将 live-audit 上海医疗补贴实测 trace 与审计 HTML 归档为项目核心测试资产：

  fixtures/live-audit/shanghai-medical-subsidy-debug-trace.json
  fixtures/live-audit/shanghai-medical-subsidy-golden-fixture-evaluation-baseline.html

该 fixture 对应的官方证据为上海普陀政府页面：

  https://www.shpt.gov.cn/zhengwu/qtczxx-wjwczxx/2026/58/201854.html

关键验证点包括：

- final decision 为 stop
- 文号为 普卫健行办〔2026〕1号
- 发布主体为 上海市普陀区卫生健康委员会
- 时间属性覆盖 2026 年度
- 存在多个对象化金额条款
- 保留“普陀区区级，不是上海全市统一补贴目录”的边界
- 保留“预算/补贴下达通知，不是公开申请指南”的边界
- shui5.cn 被标记为非官方来源并排除出最终证据链

4. Prompt 冻结说明

新增文档：

  docs/prompt-freeze.md

规则：

  src/policy-task/prompt-builder.ts 作为当前生产 baseline 的冻结资产。
  除非新的 regression trace 证明存在 prompt-level failure，否则不再调整 Heavy Prompt 逻辑。
  Infrastructure 改动必须优先通过 Golden Fixture 回归，再讨论 Prompt 变更。

三、Zero-Config Startup 可移植性补齐

1. Node 版本锁定

新增：

  .nvmrc

内容：

  v22.21.0

README 中已写明当前验证环境：

  Node.js: v22.21.0
  pnpm: 10.33.0

另一台电脑可执行：

  nvm use
  corepack enable

2. 环境变量模板

新增：

  .env.example

包含 live audit 所需最小配置：

  NANOCLAW_LLM_PROVIDER=openai
  NANOCLAW_BASE_URL=https://<your-openai-compatible-gateway>/v1/chat/completions
  NANOCLAW_API_KEY=<your-gateway-key>

可选配置包括：

  POLICY_AGENT_LLM_MODEL
  NANOCLAW_MODEL
  SEARCH_MCP_WORKER_PATH
  LIVE_AUDIT_DEBUG
  LIVE_AUDIT_DIAG

README 中已写明从零开始配置方式：

  cp .env.example .env

3. README 部署检查清单

README 已补充 Zero-config startup checklist，包括：

- Runtime versions
- Fresh install and build
- Offline Golden Fixture regression
- Live audit configuration
- Required live variables
- Dev smoke test

核心流程：

  pnpm install
  pnpm build
  pnpm test:fixture

4. 离线 Golden Fixture 回归脚本

新增测试文件：

  __tests__/fixtures/golden-live-audit.test.ts

新增 package script：

  pnpm test:fixture

该测试不需要 live credentials，也不需要联网。
它直接读取归档 trace，验证 Agent 在上海普陀 2026 医疗卫生补贴 case 中的最终证据闭环与边界控制。

5. 绝对路径依赖清理

已审计源码、脚本、测试、README、env example 中的 /Users/lingion 绝对路径残留。

执行过：

  grep -RIn '/Users/lingion' package.json tsconfig.json src bin scripts __tests__ README.md .env.example

结果无输出。

关键修正：

- src/runtime/search-mcp-tool-adapter.ts

  默认 Search MCP worker 不再指向：

    /Users/lingion/repo-downloads/search-mcp-worker-kerry/src/stdio-server.js

  改为仓库内 vendored worker：

    vendor/search-mcp/worker-entry.js

- src/app/run-policy-task.workspace.test.ts

  删除硬编码 cwd：

    /Users/lingion/repo-downloads/local-policy-agent

  改为恢复 originalCwd。

6. package start 脚本修正

原 start 脚本指向：

  node ./dist/app/index.js

但当前 tsconfig 设置 noEmit，pnpm build 不生成 dist，因此另一台电脑执行 pnpm start 会失败。

已修正为：

  pnpm live-audit

四、验证结果

1. Clean environment 模拟验证

已执行：

  rm -rf /Users/lingion/repo-downloads/local-policy-agent/node_modules /Users/lingion/repo-downloads/local-policy-agent/dist
  pnpm -C /Users/lingion/repo-downloads/local-policy-agent install
  pnpm -C /Users/lingion/repo-downloads/local-policy-agent build
  pnpm -C /Users/lingion/repo-downloads/local-policy-agent test:fixture

结果：

- pnpm install 成功
- pnpm build 成功
- pnpm test:fixture 成功

build 输出：

  > local-policy-agent@0.1.0 build /Users/lingion/repo-downloads/local-policy-agent
  > tsc -p tsconfig.json

fixture regression 输出：

  TAP version 13
  # Subtest: golden fixture proves Putuo medical subsidy trace is regression-ready offline
  ok 1 - golden fixture proves Putuo medical subsidy trace is regression-ready offline
  1..1
  # tests 1
  # pass 1
  # fail 0

2. 可移植性相关测试

已执行：

  pnpm -C /Users/lingion/repo-downloads/local-policy-agent exec tsx --test src/runtime/search-mcp-tool-adapter.test.ts src/app/run-policy-task.workspace.test.ts

结果：

  1..4
  # tests 4
  # pass 4
  # fail 0

覆盖内容：

- workspace 持久化测试不依赖本机绝对路径
- Search MCP worker 默认路径使用仓库内 vendored worker
- Search MCP adapter contract 仍然正常

3. packaging portability 测试

已执行：

  pnpm -C /Users/lingion/repo-downloads/local-policy-agent exec tsx --test __tests__/packaging.test.ts

结果：

  1..3
  # tests 3
  # pass 3
  # fail 0

覆盖内容：

- pnpm-lock.yaml 存在
- .nvmrc 存在且版本正确
- .env.example 包含必要 live 变量
- package.json 暴露 test:fixture
- start 脚本指向 pnpm live-audit
- dependency semver ranges 合规

五、另一台电脑验收方式

在另一台电脑上 clone 后，推荐验收流程：

  nvm use
  corepack enable
  pnpm install
  pnpm build
  pnpm test:fixture

如果需要运行 live audit：

  cp .env.example .env
  # 编辑 .env，填入 NANOCLAW_BASE_URL 和 NANOCLAW_API_KEY
  pnpm live-audit

最低验收标准：

- pnpm install 成功
- pnpm build 成功
- pnpm test:fixture 成功
- 不需要本机 /Users/lingion 路径
- 不需要联网即可完成 fixture 回归
- live audit 缺少 env 时应给出明确缺失配置错误，而不是静默失败

六、最终结论

local-policy-agent 当前已达到可交付状态：

- 生产 baseline 已 git 固化
- Heavy Prompt 已作为冻结资产归档
- Golden Fixture 已作为核心 regression asset 入仓
- README 已包含从零部署清单
- .nvmrc 与 .env.example 已补齐
- 本机绝对路径依赖已从源码/脚本/测试/文档中清理
- clean install + build + offline fixture regression 已验证通过
- git 工作区干净

当前可以在另一台电脑上用 pnpm install + pnpm build + pnpm test:fixture 做首次验收。
