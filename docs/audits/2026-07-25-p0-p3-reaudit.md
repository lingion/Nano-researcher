# P0–P3 / Bug / Logic / Feature / Design Audit Inventory

审计时间：2026-07-25 之后的复核阶段

## 说明

本文件把此前 transcript、计划、运行产物和目标仓库只读检查中定位到的问题先行固化。`confirmed` 表示在目标代码或真实产物中有直接证据；`historical` 表示曾发生过但后续有人声称修复，尚未在目标 HEAD 全量复验；`needs-verification` 表示计划或结构明确指出风险，但仍需最小复现；`process-gap` 表示审计/交付流程缺陷。不要把相似表现重复计数为独立根因。

## 一、模型决策与 Runtime 权限边界

1. [P0][fixed] Runtime 保留模型的 fetch-only 决策，不再将 `contextQuery` 复活为 search，也不再把 legacy `status: CONTINUE` + `fetchActions` 返回成原始 `CONTINUE`；`src/runtime/ask-real-claude.ts`；focused real-ask-agent 12/12、full 209/209、typecheck、diff-check、build 通过。
2. [P0][fixed] Runtime 不再从 payload/关键词、`stopDecision.shouldStop` 或固定 fallback query 自动生成 search action；缺少模型显式 search action 时 fail-closed 为 stop。`src/runtime/ask-real-claude.ts`；TDD regression 与 focused real-ask-agent 12/12 通过，完整验证见 inventory。
3. [P0][fixed] fetch action 不再被 Runtime 复活成 search+fetch；legacy fetch-only action containers 被识别并规范化为 `continue_fetch`，`searchActions` 保持为空。
4. [P0][fixed] search 与 fetch 已由 runtime decision/action invariant 严格互斥；目标仓库回归测试通过。
5. [P0][fixed] terminal decision 携带 action 会在工具执行前被拒绝；目标仓库回归测试通过。
6. [P0][fixed] 非法 action 组合现在以 typed protocol error fail-closed；目标仓库回归测试通过。
7. [P0][fixed] Runtime 不再让 target/date/quality/convergence gate 覆盖模型 terminal decision。此前 `post_convergence_review` 会抑制模型 `finalize`，TDD RED 在 `currentIteration=2`、`maxIterations=4`、`agentDecisionType=finalize`、`convergencePhase=post_convergence_review` 下复现 `shouldBreak=false`；最小修复为显式 `finalize`/`stop` 优先返回 `shouldBreak=true`、`interruptedByGate=false`，不恢复 runtime target gate。`src/runtime/termination-policy.ts`、`__tests__/runtime/termination-policy.test.ts`；focused 4/4、full 210/210、TypeScript no-emit、`git diff --check`、`pnpm build` 均通过。
8. [P0][historical-unverified] 历史设计文档曾提出“20 条硬门槛”，但目标仓库当前执行路径未发现固定 20 条 runtime target gate，也没有复现出它强迫继续或提前停止。`targetHotspotCount`/`targetValidatedEvidenceCount` 现在只是可配置、模型可见的 task metadata；报告层的 `validCount`/`shortfall` 不驱动 policy loop。无真实 business RED，因此不新增生产修复，也不把不存在的缺陷标记为 fixed。
9. [P0][fixed] `targetHotspotCount` 已贯穿 policy loop 的 options、首轮 state、跨轮 merge 和 prompt context；focused target-metadata/policy-loop tests 23/23、full suite 210/210、TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。target metadata 仍只是模型可见上下文，不驱动 runtime continue/stop gate。
10. [P0][fixed] `validCount` 在发现、抓取、报告链路中可能不一致；目标仓库已在 `src/artifacts/write-early-access-report.ts` 中将 fetched → validated/reportable 派生、结构校验、稳定 URL 去重、normalized target、`validCount` 与 `shortfall` 固定为明确契约，并由 `runLiveAudit` 写入 `early-access-report.md` 与 `run-summary.json`，分别暴露 discovered、fetched、validated、reported 指标。focused 28/28、full 203/203、`pnpm build`、typecheck、diff-check 均通过；依赖安装使用官方 npm registry，`allowBuilds` 已明确批准 `better-sqlite3`/`esbuild`。
11. [P0][fixed] 达不到目标时 shortfall 没有稳定产生；目标仓库现在由 `writeEarlyAccessReport` 统一计算 `shortfall = max(0, normalizedTarget - validCount)`，并同步写入 Markdown、`run-summary.json` 与 `runLiveAudit` 返回值。已覆盖空结果、部分满足、目标满足、非法/小数 target，以及 live-audit `target=2`、`validCount=1`、`shortfall=1`；focused 28/28、full 203/203、`pnpm build`、typecheck、diff-check 均通过。
12. [P1][fixed] context governor 参与业务筛选、排序或裁剪；目标仓库当前 `governLLMActiveView`/`pruneDiscoveryContext` 只规范化文号，不按业务分数排序、不按 `maxGeneralCandidatesCount` 截断，也不因 current-turn anchor 丢弃候选。`run-policy-task` 仅把它用于模型可见 state 的结构规范化；回归测试验证全部候选、输入顺序和非 anchor 候选均保留，`candidateEvidenceScore` 未进入生产裁剪路径。context-governor 回归、full 203/203、`pnpm build`、typecheck、diff-check 均通过；依赖安装使用官方 npm registry 完成。
13. [P0][fixed] 工具层的 `waitlist`/`invite_only`/`public_release` 仅作为页面观察到的 `accessSignals` metadata，不直接产生模型 `access_status` 或 stop/continue/finalize 结论；原先 freshness 固定 `2026-04-01` 的问题已修复为沿 `run-policy-task -> fetchWithLocalPrimary -> fetchWithBrowserFallback` 传递调用方 `dateWindow`，并据此计算 `in_window/out_of_window`。browser-fetch、local-fetch-primary、policy-loop focused tests 26/26，`pnpm build`、typecheck、diff-check 均通过。
14. [P1][fixed] local fetch 与 MCP fetch 已共用 `src/fetch-fusion/evidence-clues.ts` 的 `detectSuspectedReprint`，MCP 不再无条件伪造 `false`；标题/正文转载信号与非官方 news/media/reprint URL 的回归已通过。该字段仍是观察性 clue，不生成 access status、inclusion 或模型 decision；focused 15/15、full 205/205、build、typecheck、diff-check 均通过。
15. [P1][fixed] MCP adapter 已对外部 `access_source_grade` 做 runtime enum 校验：非法值丢弃、合法值保留；该字段仍只作为搜索候选排序 hint，不生成 access status、inclusion 或模型 decision。focused 9/9、full 206/206、build、typecheck、diff-check 均通过。
16. [P0][open] 工具层将 inclusion 结论伪装为 transport fact。

## 二、Provider / Config / Portability

17. [P1][historical] 任意 OpenAI-compatible gateway URL 被隐式当作 Anthropic provider。
18. [P1][historical] provider hard-lock 导致错误 API protocol。
19. [P1][historical] preflight 空响应被错误判为失败。
20. [P1][fixed] preflight 瞬时网络错误现在使用有界 retry attempts、可注入 backoff delay 和 AbortSignal-aware backoff；仅 retryable runtime/network error 重试，永久错误保持 fail-fast，恢复成功后才进入 policy loop。目标路径：`src/app/live-audit-runtime.ts`；回归：`__tests__/app/run-live-audit.test.ts`。
21. [P1][fixed] preflight 网络失败会写入 `live_audit.preflight.error`、`run.failure` 和 durable debug trace，并阻止进入 policy loop；目标仓库 ECONNRESET 回归通过。
22. [P1][needs-verification] 配置数值用 `parseFloat` 但没有 finite/range 校验。
23. [P1][historical-unverified] Provider schema 当前只定义 `enabled`、`api_key`、`base_url`，不存在原 finding 所述 `type/settings` 字段；已注册 provider 名称会被校验，未知字段虽可能被忽略但属于独立 schema 严格性问题。目标路径：`vendor/search-mcp/src/mcp/tool-schemas.js`、`vendor/search-mcp/src/index.js`、`vendor/search-mcp/src/local/provider-config-store.js`。
24. [P1][historical-unverified] Provider key 解析按 provider 独立隔离：请求头 `x-${name}-api-key`、该 provider 的配置 `apiKey`、该 provider 明确传入的环境变量名依次回退；`PROVIDER_DEFAULTS` 各 provider 的 `apiKey` 为空，未发现缺失 key 时恢复其他 provider backup key 的路径。目标路径：`vendor/search-mcp/src/index.js:56`、`vendor/search-mcp/src/index.js:519`、`vendor/search-mcp/src/index.js:4491`、`vendor/search-mcp/src/core/provider-defaults.js`。原 finding 在当前 HEAD 无法复现。
25. [P1][fixed] fallback model 与 primary model 共用 NanoClaw provider/baseURL/API key；gateway `/models` preflight 现在同时探测并记录 primary/fallback 的 `configuredModels` 与 `includedConfiguredModels`，fallback 请求仍只在 primary 失败后触发，不改变模型业务决策。focused runtime/app 38/38、full suite 211/211、TypeScript no-emit、`git diff --check`、`pnpm build` 均通过。
26. [P2][historical-unverified] 原 finding 声称 Dexie Cloud 被禁用但未清理旧同步状态；目标仓库当前 HEAD 的 package/lockfile、src、tests、配置和迁移文件均未发现 Dexie/Dexie Cloud 或旧同步状态实现，命中的 `sync` 仅为 SQLite/文件持久化 fsync 或应用内部 ACK 同步，无法复现，未新增生产修复。
27. [P2][historical-unverified] 历史描述称 `pnpm start` 指向不存在的 `dist/app/index.js`；目标仓库当前 HEAD 的 `package.json` 已将 `start` 委托给 `pnpm live-audit`，后者执行 `tsx src/app/run-live-audit.ts`。实际运行 `pnpm start -- --help` 成功进入并完成 live-audit，未加载 `dist/app/index.js`，原 finding 无法复现，未新增生产修复。
28. [P2][historical-unverified] 历史描述称 ignored install scripts 可能导致 better-sqlite3/esbuild 二进制缺失；目标仓库当前 `pnpm-workspace.yaml` 已明确 `allowBuilds` 允许 `better-sqlite3` 与 `esbuild`，且当前安装可加载 better-sqlite3 并完成内存 SQLite 建表、写入和查询 smoke test，原 finding 无法复现，未新增生产修复。
29. [P2][historical-unverified] 历史描述称缺少环境变量时诊断信息不清晰或静默失败；目标仓库当前 `resolveNanoclawRuntimeConfig` 对缺少 API key/base URL 明确抛出缺失变量错误，运行 live-audit 时记录 `live_audit.preflight.error` 与 `run.failure`，并明确说明在进入 `runPolicyTaskLoop` 前失败，原 finding 无法复现，未新增生产修复。
30. [P2][historical-unverified] 历史描述称隐式 binary/provider/env 依赖破坏跨机器可移植性；当前 HEAD 提供 `.nvmrc` Node 22.21.0、pnpm lockfile、显式 semver 依赖、`.env.example` provider/key/base URL 说明、offline fixture/portable start scripts，并以 `allowBuilds` 明确 native build policy。现有 packaging 回归验证这些 portability metadata，未复现跨机器安装或启动失败，未新增生产修复。

## 三、Search / Discovery / Ranking

31. [P1][fixed] SearXNG search 没有可靠 retry/backoff：provider 层现在限制 1–3 次总尝试，仅对 retryable runtime/network error 做 bounded retry/backoff；non-retryable failure 不重试，retry exhaustion 后继续后续 provider，backoff 可由 AbortSignal 中断。WebSearch 与 HTML fallback 仍是独立调用/顺序 fallback，未宣称共享该 retry contract。
32. [P1][fixed] 一次 search 失败可能拒绝整个 batch：provider 层对普通非取消错误执行 failure isolation，保留后续 provider 结果；AbortSignal cancellation 仍 fail-fast。该隔离不扩张为 WebSearch/HTML fallback 的统一 retry 语义。
33. [P1][fixed] 三层均无结果时返回 `quality_state.status: 'empty'`，不再抛出 Search Fatal；目标仓库回归测试与完整验证通过。
34. [P1][open] 没有自动 query refinement。
35. [P1][open] 搜索结果没有逐条 URL/title/source 校验。
36. [P2][open] 没有 canonical URL 去重。
37. [P2][open] 没有稳定 search candidate ID。
38. [P2][open] 没有 discovery timestamp 持久化。
39. [P2][open] 没有 content hash。
40. [P1][open] fusion 前 `resultsLimit` 截断候选。
41. [P2][confirmed] 媒体/知乎/36Kr 可能压过官方来源。
42. [P2][open] 没有官方域名优先级。
43. [P1][confirmed] 全国任务被路由为黑龙江地方政策任务。
44. [P1][confirmed] OPC 等缩写没有先做 disambiguation。
45. [P1][open] national/provincial/city scope 没有强约束字段。
46. [P1][confirmed] Heavy Prompt 过拟合地方政策素材。
47. [P1][confirmed] 定义研究没有转换到政策搜索。
48. [P1][confirmed] 官方候选出现后没有转换到 fetch。
49. [P1][confirmed] 多轮 continue_search 后仍无阶段转换。
50. [P1][confirmed] maxIterations 消耗在研究阶段，打断后续验证。

## 四、Fetch / Browser / Preprocessing

51. [P1][open] 抓取主要是 Jina wrapper，不是完整 fetch/browser 融合。
52. [P0][open] 真实 MCP fetch 路径可能绕过 Browser fallback。
53. [P2][needs-verification] Browser 失败后静态弱内容仍可能被当成证据。
54. [P2][open] fallback warning、尝试链和最终来源结构不统一。
55. [P1][open] URL 没有 allowlist。
56. [P1][open] 没有 SSRF/private-IP 防护。
57. [P2][open] 没有统一 fetch timeout。
58. [P2][open] 没有 response size 上限。
59. [P2][open] 没有 Content-Type 校验。
60. [P1][open] Promise.all 使一个 crawl 失败影响全部 crawl。
61. [P1][open] challenge/login/WAF 页面可能被当正文。
62. [P1][confirmed] `normalizeFetchedPage` 丢失 raw/date/metadata/attempt facts。
63. [P0][open] 日期解析结果被提前变成 inclusion 业务结论。
64. [P2][open] HTTP status、redirect chain、final URL 未完整传播。
65. [P2][open] static→browser→fallback 尝试链未持久化。
66. [P1][open] citation 未验证是否来自正文。
67. [P1][open] citation 未验证是否支持结论。
68. [P1][open] publication date 与 update/access date 可能混淆。
69. [P2][historical] 取正文最后一个日期可能选错日期。
70. [P2][historical] 非法日期过滤曾缺失。
71. [P2][open] Playwright/browser 资源关闭不稳定。
72. [P2][open] MCP worker stdio/network shutdown 不完整。

### F-063 复验闭环：报告日期 freshness 仅使用结构化日期

- **问题**：`deriveEarlyAccessItems` 曾把 `publishedAt`、`updatedAt`、标题和正文拼接后交给通用 `classifyDate`。正文或页脚中的窗口内日期可能覆盖窗口外结构化日期，并直接让页面进入 early-access report。
- **最小复现**：`__tests__/artifacts/write-early-access-report.test.ts` 覆盖结构化旧日期 + 正文窗口内日期、结构化窗口内日期 + 正文旧日期、仅正文日期以及窗口起止日。旧实现下前两类边界中的错误日期会影响结果，body-only 日期会错误产生 `in_window`。
- **修复**：report boundary 仅传入 `page.updatedAt ?? page.publishedAt ?? ''`；不修改通用 `classifyDate`，不把正文日期作为发布日期来源。`updatedAt` 优先，缺少结构化日期时保持 `date_unknown`。
- **边界保持**：`scoreEarlyAccessSignals(title + content)`、positive signal gate、质量等级和 canonical URL 去重保持不变。freshness 只生成 metadata，不生成 `access_status`、`test_type`，也不覆盖模型 stop/continue/finalize 决策。
- **验证**：RED 回归在旧实现下按预期失败；修复后 artifact 与 live-audit 相关测试 31/31，完整 suite 228/228，TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
- **状态**：fixed。其他编码/压缩、challenge/login、静态与 browser 合并、多来源日期冲突等独立遗漏不因本 finding 自动关闭。


- **问题**：`normalizeCompositeDecision` 曾从 fetch action 的 `contextQuery` 复活 search action，并以 search action 是否存在推导 `continue_search`，导致模型的 fetch 意图被 Runtime 改写。
- **最小复现**：`__tests__/runtime/real-ask-agent.test.ts` 注入 `status: SEARCH_AND_FETCH` 与仅含 `fetchActions`、`contextQuery` 的模型 payload；修复前返回 `continue_search` 并伪造 search action。
- **修复**：删除 fetch→search revival；fetch-only action 保持 `continue_fetch`，不再由 `contextQuery` 生成 search action。
- **验证**：focused real-ask-agent 12/12；full suite 209/209；TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
- **状态**：fixed。

### F-052 复验闭环：真实 MCP fetch 与 Browser fallback 组合

- **问题**：默认 loop 曾把本地 fetch 结果改写为 `search-mcp:fetch_url`，而 `enableBrowser` 分支又绕过实际 MCP fetch；真实 MCP 弱内容因此不会进入 Browser fallback。
- **最小复现**：`__tests__/app/run-policy-task.test.ts` 注入一个弱 MCP fetch（`Please enable JavaScript`），并注入 `BrowserAdapter`；修复前 `browserCalls` 为 `0`，focused 测试失败。
- **修复**：`runPolicyTaskLoop` 新增类型化的 `browserAdapter` 注入，并用 `fetchWithBrowserFallback` 包装实际 `baseFetchTool`；默认 `createDefaultToolset` 改为直接调用 `createSearchMcpTools()`，不再给本地 fetch 伪造 MCP backend label。
- **验证**：F-052 focused app/runtime regression 19/19；TypeScript no-emit、`git diff --check` 通过。默认 MCP-owned path 既有回归通过；full suite 209/209、TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
- **状态**：fixed（业务修复、全量回归、类型检查、差异检查与构建均通过）。

## 五、Model Decision / Protocol Schema

73. [P0][fixed] malformed model output 现在抛出 `ProtocolDecisionError` 并 fail-closed；`src/runtime/ask-real-claude.ts` 与目标仓库回归测试已验证。
74. [P0][fixed] invalid action 不再被丢弃后继续执行，混合 action 整体失败；`src/runtime/decision-protocol.ts` 与目标仓库回归测试已验证。
75. [P0][fixed] decision/action 一致性已由 parser/runtime invariant 强制执行；目标仓库回归测试通过。
76. [P1][open] 没有 bounded retry/termination state。
77. [P1][open] confidence 没有统一 schema/阈值语义。
78. [P1][open] citation requirement 只验证文本、不验证事实。
79. [P1][open] protocol error 与 transport error 没有稳定区分。
80. [P1][open] 工具错误可能被解释为继续搜索或结束业务。
81. [P2][needs-verification] output schema 允许 arbitrary keys，legacy normalization 可能改变语义。
82. [P1][open] malformed decision 消耗 iteration 后可能产生 ambiguous final result。

## 六、Runtime Loop / Retry / Concurrency / Cancellation

83. [P1][fixed] askAgent failure 不再在 `state.updated` 前直接抛出而无状态投影；`src/runtime/local-session-loop.ts` 现在发出 `agent.failure` 与带 `runtimeFailure` 的 `state.updated`，保留原始错误且不伪造 `agent.decision`。askAgent rejection RED、focused regression、full suite 213/213、TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
84. [P2][fixed] ask-agent failure 与 search/fetch failure 已有明确的事件/状态投影边界；agent failure 不写入 transport evidence，工具失败继续使用 tool-specific failure 与 transport outcome。相关 runtime regression、full suite 213/213、TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
85. [P2][fixed] loop 已具备统一 explicit `runtimeFailure` state 字段；失败路径写入 stage 与序列化错误并发出 `state.updated`，不生成成功 decision。相关 runtime regression、full suite 213/213、TypeScript no-emit、`git diff --check` 与 `pnpm build` 均通过。
86. [P2][open] search/fetch/agent 没有统一 retry taxonomy。
87. [P2][open] 缺少 stage-level timeout。
88. [P2][open] 缺少 heartbeat。
89. [P1][open] 缺少 durable terminal artifact。
90. [P2][open] clean shutdown 可能丢失 trace/evidence/report。
91. [P1][historical] trace persistence error 曾覆盖原始 loop error。
92. [P2][historical] debug event 新序列破坏旧测试契约。
93. [P2][open] debug event contract 没有版本化。
94. [P2][open] state/event/report 没有同一提交点保证。
95. [P2][open] 中断恢复没有明确 replay/idempotency 规则。
96. [P1][open] 同批任务缺少失败隔离。
97. [P2][fixed] Promise-form operation timeout 不具备通用取消能力；`withTimeout` 现在支持 signal-aware operation，并在 MCP connect timeout 的 cleanup 路径显式关闭 transport；目标仓库 focused runtime/MCP 回归、full suite、TypeScript no-emit、`git diff --check` 与 `pnpm build` 通过。
98. [P2][fixed] browser-enabled fetch 已统一经过 `createPersistentFetchTool`；目标仓库 workspace 回归测试通过。
99. [P1][fixed] transport failure 已投影 `transportOutcome` 并阻断 finalize；目标仓库 policy-loop 回归测试通过。
100. [P1][fixed] `convergencePhase`/`targetValidatedEvidenceCount` 已初始化并跨轮传播；目标仓库回归测试通过。

## 七、Evidence / Provenance / Dedup / Persistence

101. [P1][open] 没有统一 evidence identity。
102. [P1][open] 结论与原始证据没有强绑定。
103. [P2][open] runtime 不一定复用 workspace dedup 规则。
104. [P2][open] 没有跨轮次去重。
105. [P2][open] 没有跨来源内容去重。
106. [P1][confirmed] `is_suspected_reprint` 被硬编码/直接设 false。
107. [P1][open] 日期窗口没有统一入口。
108. [P1][open] 普通发布新闻可能误计为 early access。
109. [P2][open] 没有记录页面版本/抓取版本。
110. [P2][open] 没有记录 evidence retrieval time。
111. [P2][open] 候选淘汰原因没有稳定保存。
112. [P2][open] transport attempt facts 没有形成可恢复 journal。
113. [P1][process-gap] 计划要求 append-only journal，但 canonical findings store 不存在。
114. [P1][open] recovery 后可能重复抓取、重复计数或重复报告。

## 八、Reporting / Metrics / Shortfall / Citation

115. [P0][fixed] live audit 现在从 `result.fetchedEvidence` 统一派生 early-access items，调用 report writer 并写入逐条 `early-access-report.md`；focused report/live-audit 回归 28/28 通过。当前证据基于 dirty 工作树；`writeFileSync` 外部 I/O 失败属于独立持久化风险，未被本 finding 吞并。
116. [P0][fixed] summary、返回值与逐条报告均复用同一 `earlyAccessReport` 的 validCount/target/shortfall，focused 回归验证一致性。当前证据基于 dirty 工作树，尚需 full/build 在目标提交或干净快照上复核。
117. [P1][confirmed] `status: complete` 只证明流程完成，不证明业务正确。
118. [P1][open] 没有来源覆盖率自动检查。
119. [P2][open] 没有申请入口可用性验证。
120. [P1][open] 没有候补/邀请状态的当前时效验证。
121. [P2][open] 没有统一报告证据过期原因。
122. [P2][open] shortfall 没有逐条原因。
123. [P2][open] 没有报告每条证据抓取时间。
124. [P2][open] 没有报告证据版本。
125. [P1][open] citation-to-claim 映射未验证。
126. [P2][fixed] report writer 在边界重验 target/items：规范化 target，过滤 malformed/空字段，按 canonical identity 去重；业务语义验证仍由 `deriveEarlyAccessItems` 负责。focused report/live-audit 回归 28/28 通过，证据基于 dirty 工作树。

## 九、Live Audit / MCP / Operational Lifecycle

127. [P2][confirmed] startup live update/checkUpdate 路径被显式禁用。
128. [P2][needs-verification] migration failure 仅记录并忽略，没有 user-visible audit status。
129. [P2][fixed] MCP close 现在有 bounded timeout；policy-loop cleanup failure 会发出 `mcp.cleanup.failure` 且不覆盖原始 run error，main 路径也记录 cleanup failure。目标仓库已通过 close rejection/timeout、main cleanup、主错误优先级以及 `live.log`/`debug-trace.json` durable event 回归。目标路径：`src/runtime/search-mcp-tool-adapter.ts`、`src/app/run-live-audit.ts`、`src/app/run-policy-task.ts`。
130. [P2][historical-unverified] 当前目标仓库未发现带 pool entry 的 MCP `onclose` 实现；该问题目前只有历史线索。
131. [P2][historical-unverified] 历史线索称 `mcp-client.ts` 会 mutate caller settings、删除 timeout，但当前目标 HEAD 不包含该文件或对应调用路径；当前 `src/runtime/search-mcp-tool-adapter.ts` 没有 caller settings，也没有 `delete settings.timeout`。未新增伪造复现或修复；恢复历史实现后需用包含 timeout 的 settings 建立 caller immutability regression。
132. [P2][fixed] MCP `client.connect(transport)` 现在由独立 `connectTimeoutMs`/`LIVE_AUDIT_MCP_CONNECT_TIMEOUT_MS` bounded；deterministic stdio regression 已验证 hanging connect 在 50ms 后失败，cleanup 后 `transport.pid === null` 且原 child pid 已退出（`process.kill(pid, 0)` 返回 `ESRCH`）。目标路径：`src/runtime/search-mcp-tool-adapter.ts`；回归：`src/runtime/search-mcp-tool-adapter.test.ts`。
133. [P1][historical] vendored Search MCP stdio 曾 connection closed。
134. [P1][open] worker entry/config/protocol 缺少可移植性验证。
135. [P2][open] MCP startup failure diagnostics 不足。
136. [P2][open] live audit 没有稳定 heartbeat/progress artifact。
137. [P2][needs-verification] `LIVE_AUDIT_RUN_TIMEOUT_MS` 现在包裹完整 preflight + policy loop，超时写入 `live_audit.run.timeout`，并有 signal-aware adapter/policy-loop regressions。当前普通 Promise 语义是停止等待而非保证底层取消；global WebSearch/WebFetch 的底层取消能力及成功 production build 仍未建立。目标路径：`src/app/live-audit-runtime.ts`。
138. [P1][open] preflight、loop、report、terminal artifact 之间没有统一 run ID 关联。

## 十、安全 / SSRF / Prompt Injection / Secrets / Supply Chain

139. [P1][open] 任意 crawl URL 没有 SSRF/private-IP 防护。
140. [P1][open] MCP stdio command/env/cwd 可以直接执行本地命令。
141. [P1][open] shell command 通过 `sh -c`，没有 allowlist/确认边界。
142. [P1][open] MCP headers/env schema 允许敏感或危险字段。
143. [P1][open] 外部抓取文本直接进入模型上下文，存在 prompt injection。
144. [P1][open] 没有将抓取内容显式隔离为 untrusted data。
145. [P2][open] CDN code-exec 没有 SRI/integrity pin。
146. [P2][open] runtime package loading 没有版本策略或 offline fallback。
147. [P1][needs-verification] `.env.live` 明文 API key 的本地暴露边界已加固：`.env.*` 保持忽略，新增 packaging regression 确认 `.env.live`/`.env` 未被 git 跟踪，README 明确 live credential 必须由外部运行环境注入且不得进入源码、测试、报告或日志。未读取或修改凭据值，也未执行 provider-side 撤销/轮换；该 finding 不能标记 fixed，待明确授权后完成轮换并重新验证。
148. [P1][needs-verification] debug/log 持久化边界已接入共享递归 sanitizer：错误仅保留安全 metadata，search/fetch 仅保留计数与安全摘要，URL 清除 userinfo/query/fragment，prompt/state/raw model output、headers、正文、stack 和 diagnostics 不进入 debug projection；业务 state、evidence、report、business transcript 与有效 outbound message 保持完整。provider、poll-loop、MCP diagnostics 已改为固定类别或安全 metadata。focused/full/build/typecheck/diff-check 已通过；历史日志和 provider-side artifacts 未清理，需独立复核部署边界。
149. [P1][needs-verification] 没有统一 token/cookie/header 脱敏。当前 HEAD 已收紧 runtime debug serializer，并修复 vendored search-MCP JSON-RPC internal error 与 stdio startup failure 的原始错误透传；正常 transport Authorization header 和业务 search/fetch 结果保持不变。仍需独立复核所有 vendor result/error/artifact 路径及历史/provider-side 边界。
150. [P2][open] plugin result 没有 size cap，可能把过大 payload 写入 IndexedDB。

## 十一、Tests / Packaging / Deployment / Audit Process

151. [P2][process-gap] 直接运行 Vitest 失败，项目实际测试入口是 `tsx --test`。
152. [P2][process-gap] `pnpm exec tsx` 曾触发 registry 安装，造成离线环境误判。
153. [P1][process-gap] 多个代理在错误 worktree，修复没有回到目标仓库。
154. [P1][process-gap] 错误 worktree 产生的测试文件不能作为目标修复。
155. [P1][process-gap] 没有统一 cherry-pick/commit 回收机制。
156. [P2][process-gap] 测试结果没有绑定 commit/dirty snapshot。
157. [P2][process-gap] 没有 race test 证据。
158. [P1][process-gap] 没有完整静态审计闭环。
159. [P0][process-gap] 计划引用的 `docs/audits/2026-07-25-p0-p3-reaudit.md` 未落盘。
160. [P1][process-gap] P0/P1/P2/P3 文字计数曾被误解为 finding 数量。
161. [P1][process-gap] 没有 finding ID。
162. [P1][process-gap] 没有 open/fixed/verified/duplicate lifecycle。
163. [P1][process-gap] 没有 remediation evidence。
164. [P1][process-gap] 没有 re-audit 结果。
165. [P1][process-gap] 没有 section coverage matrix。
166. [P1][process-gap] 当前 HEAD、历史 worktree、代理声明没有清晰分层。
167. [P2][process-gap] dirty working tree 影响可重复性。

## F-062 复验记录

`normalizeFetchedPage` 的 RED 回归先复现了 browser/local fetch normalization 丢失 optional provenance、日期、渲染、质量与验证字段的问题。目标仓库现已用显式 allowlist 保留 `FetchedPageRecord` 已定义的 optional fields，同时保持默认 `evidence_clues` 和 `kerry_cleaning` 行为不变。验证结果：focused local-fetch 测试 8 passed；完整测试 212 passed / 0 failed；`tsc -p tsconfig.json --noEmit` exit 0；`git diff --check` exit 0；`pnpm build` exit 0。F-062 已标记 fixed。


目前固化了 167 条独立记录，其中包含 confirmed、historical、needs-verification、design/open 和 process-gap；这不是“167 条全部已确认生产 bug”。每条记录都需要在机器清单中保持状态和证据类型分离。后续复审将以这些 section 为基线，每个 section 继续检查输入边界、空值、部分失败、重试预算、并发、取消、幂等恢复、状态一致性、证据追踪、模型-owned/system-owned 边界和安全权限，不通过拆分同一根因来凑 300 条。

## 修复前的历史复现记录

以下记录描述目标仓库在本轮产品修复之前完成的最小复现；对应问题中已具备完整代码、回归测试和复验命令证据的项目已在上方“已验证修复（目标仓库）”及机器清单中标为 `fixed`。这些历史记录不应被解释为当前 HEAD 仍然存在同样缺陷。

- malformed model decision fail-open：`src/runtime/ask-real-claude.ts:569-581` 与 `src/app/run-policy-task.ts:81-118`；模型固定返回 `{` 时，`maxIterations=3` 仍执行 3 次，产生 `INVALID_JSON` 但不 fail-closed。
- invalid action drop：`src/runtime/decision-protocol.ts:62-74,103-121`；bad URL action 被丢弃，valid sibling 仍执行，结果 `ok=true` 并带 `INVALID_ACTION`。
- terminal decision/action inconsistency：`src/runtime/local-session-loop.ts:112-156`、`src/app/run-policy-task.ts:114-115`；finalize/stop/summarize envelope 携带 fetch action 时先 fetch 再 terminal return，且 `final_package` 一致性未由 parser 强制。
- transport failure 不降低最终质量：`src/runtime/local-session-loop.ts:48-82,157-165`；search/fetch 抛错只进入 `transportFacts`/debug event，仍可接受 finalize。
- Promise timeout 不取消底层操作：`src/runtime/reliability.ts:20-38`；signal 被 abort，底层 Promise 在 timeout 后仍 settled。
- state 初始化缺口：`src/app/run-policy-task.ts:62-68,100-109`；`convergencePhase` 与 `targetValidatedEvidenceCount` 未进入 loop state，而 prompt contract 使用它们。
- browser path 绕过 persistence：`src/app/run-policy-task.ts:73-75` 与 `src/workspace/persistent-fetch-tool.ts:10-13`；`enableBrowser=true` 直接走 fetch fusion，未经过 `saveFetchResult`。

验证基线、命令和环境限制见 `docs/audits/2026-07-25-verification-baseline.md`。这些 finding 目前只确认缺陷存在，不代表已经修复。

## 已验证修复（目标仓库）

F-004、F-005、F-006、F-021、F-033、F-073、F-074、F-075、F-098、F-099、F-100 已在目标仓库 HEAD `fdf793ee2873b08af065643172c06fd838a949b` 上完成代码、回归测试和验证命令三重核对。F-031 已补齐 provider 层 bounded retry/backoff：总尝试次数限制为 1–3，仅 retryable runtime/network error 重试，non-retryable failure 不重试，retry exhaustion 后继续后续 provider，backoff 可由 AbortSignal 中断；WebSearch 与 HTML fallback 仍不共享该 provider-only retry contract。F-032 已补齐 aggregate provider failure isolation：单个 provider 的非取消错误不会丢弃后续 provider 结果，AbortSignal 取消仍保持 fail-fast。F-097 已固定：普通 Promise timeout 不被误声明为通用取消，signal-aware operation 与 MCP cleanup 分别提供真实取消/资源收敛证据。


## 待验证优先级

1. 先复现 malformed decision fail-open、invalid action drop、context governor ignored limits、non-signal timeout cancellation。
2. 再确认 browser path 是否绕过 persistence，以及 transport failure 是否进入最终质量状态。
3. 再检查 provider backup key 恢复、MCP close/connect race、URL/SSRF、plugin payload size。
4. 最后运行目标仓库测试、静态检查和可重复性检查，写入命令、退出码、commit/dirty snapshot。

## 证据边界

本文件不复制 token、API key、cookies、完整 transcript 或敏感页面正文。错误 worktree 中的路径仅作为历史审计线索，不能证明目标仓库已修复。任何“fixed”状态必须由目标仓库代码、测试和复验命令共同证明。

## 判断疏漏复审矩阵

以下不是把同一根因重新计数为 bug，而是记录前一轮审计可能漏掉的检查角度。每项均标为 `needs-verification`，除非目标仓库 HEAD 上已有直接复现证据。

### 1. Provider / Config / Portability

- **遗漏 1：配置来源优先级未审计。** 需要确认 env、stored provider、CLI 参数和 fallback 的覆盖顺序；重点路径：`src/utils/config.ts`、`src/stores/providers.ts`。验证：用冲突配置启动并记录最终 provider/model。
- **遗漏 2：数值配置的单位/时区未审计。** timeout、recency days、max steps 可能分别按秒、毫秒或自然日解释。验证：对边界值和跨日时间运行最小测试。
- **遗漏 3：空字符串与缺失值未区分。** 空 URL、空 key、空 model 可能绕过缺失配置校验。验证：逐项传入 `undefined`、`null`、`""`、空白字符串。
- **遗漏 4：多 provider 并发隔离未审计。** 一个 provider 的限流、abort 或错误是否污染另一个 provider 尚未证明。验证：并行注入一个失败 provider 和一个成功 provider。
- **遗漏 5：升级/降级配置兼容未审计。** 旧 settings schema、未知字段和默认值迁移未形成可回放证据。验证：用历史配置 fixture 运行加载、保存、再次加载。

### 2. Search / Discovery / Ranking

- **遗漏 1：搜索语义与任务 scope 的绑定未审计。** query 可能找到相邻概念但没有国家/产品/访问状态约束；重点路径：`src/utils/web-search-plugin.ts`、`src/search-fusion/auto-router.ts`。
- **遗漏 2：排名稳定性未审计。** 同一结果集在不同 engine 顺序、空 snippet 或缺 title 时是否改变排序未验证。验证：固定 fixture 重复运行并比较顺序。
- **遗漏 3：分页/截断边界未审计。** `resultsLimit`、offset、重复结果和最后一页的处理可能遗漏候选。验证：用恰好 limit、limit+1、重复 URL 的 fixture。
- **遗漏 4：搜索结果的来源时间未审计。** engine 返回时间、页面发布时间和本次检索时间可能混用。验证：保留三种时间并检查最终 evidence 字段。
- **遗漏 5：部分 engine 成功时的质量门槛未审计。** 一个 engine 成功是否掩盖其他 engine 失败没有统一规则。验证：逐个模拟 timeout、空结果、畸形 JSON。

### 3. Fetch / Browser Fallback / Preprocessing

- **遗漏 1：重定向后安全性未审计。** 初始 URL 合法不代表最终 URL 合法，需检查每跳协议、host 和私网地址；重点路径：`src/fetch-fusion/browser-fetch.ts`、`src/utils/web-search-plugin.ts`。
- **遗漏 2：编码和压缩响应未审计。** 非 UTF-8、gzip、超大 HTML 和截断正文可能产生错误日期或错误结论。验证：用不同 content-type/encoding fixture。
- **遗漏 3：登录墙与挑战页的误阳性未审计。** 页面标题正常但正文是 challenge/login 时，extractor 是否拒绝尚不明确。验证：保存脱敏 challenge fixture 并检查 provenance。
- **遗漏 4：静态与 browser 内容合并规则未审计。** browser 只补充部分正文时，旧 metadata、日期和 clues 是否失效未证明。验证：构造静态旧日期 + browser 新正文 fixture。
- **遗漏 5：解析失败后的证据等级未审计。** 只得到 title/snippet 时是否仍可进入 validated evidence 未明确。验证：逐级删减正文并检查 gate。

### 4. Model Decision / Protocol Schema

- **遗漏 1：未知 decision 枚举值未审计。** schema 可能接受字符串但执行器按 `switch` 静默走默认分支；重点路径：`src/runtime/decision-protocol.ts`、`src/runtime/ask-real-claude.ts`。
- **遗漏 2：重复 action、空 action 与 null action 的组合未审计。** 这些输入可能造成重复抓取或空转。验证：构造协议 fixture 并检查 protocol error 与执行次数。
- **遗漏 3：字段类型边界未审计。** `maxResults`、`reason`、`citations` 的错误类型是否 fail closed 尚未证明。验证：传入数组、对象、NaN-like 字符串和超长字符串。
- **遗漏 4：协议版本兼容未审计。** legacy normalization 是否把旧版本合法语义改成当前语义仍需逐版本 fixture。
- **遗漏 5：模型拒绝/不确定决定的状态语义未审计。** `stop`、`finalize`、`summarize_and_stop` 的差异是否在 output schema 和报告中保持一致未证明。

### 5. Runtime Loop / Retry / Concurrency / Cancellation

- **遗漏 1：同一 run 的重复消息未审计。** queued message、retry 和 reconnect 可能重复执行同一个 action；重点路径：`src/runtime/local-session-loop.ts`。
- **遗漏 2：取消后的 late result 未审计。** 已取消的 search/fetch 若晚到，是否仍能写 state/report 未证明；关联：`src/runtime/reliability.ts`。
- **遗漏 3：并发上限与 backpressure 未审计。** 多个 fetch、browser page 和 MCP call 的资源上限没有一组可复验指标。
- **遗漏 4：retry 是否重复消耗业务预算未审计。** transport retry 可能同时消耗 iteration、token、结果配额。验证：固定失败次数并比较各预算计数。
- **遗漏 5：异常清理顺序未审计。** abort、timeout、persist、close 的先后可能造成半写 state 或泄漏句柄。验证：在每个 await 点注入异常并检查 terminal artifact。

### 6. Evidence Provenance / Dedup / Persistence / Recovery

- **遗漏 1：同 URL 不同页面版本未审计。** canonical URL 不能替代 content hash/version；重点路径：`src/workspace/report-manager.ts`、fetch fusion。
- **遗漏 2：同一事实多来源冲突未审计。** 官方与转载日期/访问状态冲突时没有明确保留两者及裁决理由。
- **遗漏 3：证据删除/替换后的引用完整性未审计。** workspace cleanup 或重新抓取后，report citation 是否仍能定位原始 evidence 未证明。
- **遗漏 4：恢复点的原子性未审计。** state 已写但 evidence journal 未写，或反向情况是否可检测尚不明确。
- **遗漏 5：重复恢复的幂等键未审计。** run ID、candidate ID、fetch attempt ID 是否足以阻止重复计数需最小 crash/restart 复现。

### 7. Reporting / Metrics / Shortfall / Citation

- **遗漏 1：统计分母定义未审计。** candidate、fetched、validated、reported 的计数是否混用需对照 `src/artifacts/write-early-access-report.ts`。
- **遗漏 2：空报告与 shortfall 的互斥关系未审计。** 零条结果、部分结果、目标未满足时应分别输出什么未形成 fixture。
- **遗漏 3：报告生成失败的状态未审计。** markdown 写入成功但 HTML/JSON 失败时最终状态是否仍为 complete 未证明。
- **遗漏 4：引用顺序和重复引用未审计。** 相同 URL 多次引用、无标题 URL、失效 URL 的呈现规则未验证。
- **遗漏 5：报告快照与运行状态的时间点未审计。** report 可能早于最后一轮 state/event 落盘；验证：注入写入延迟并比较时间戳。

### 8. Live Audit / MCP / Operational Lifecycle

- **遗漏 1：MCP capability negotiation 未审计。** server 宣布的 tool/schema 与客户端实际调用参数不一致时是否 fail closed 未证明；重点路径：`src/utils/mcp-client.ts`。
- **遗漏 2：重连后的 session/tool state 未审计。** reconnect 是否复用过期 toolCall、旧 headers 或旧 timeout 未验证。
- **遗漏 3：进程退出码与业务 terminal state 未审计。** MCP/worker 非零退出是否必然进入 failed/degraded artifact。
- **遗漏 4：健康检查的陈旧性未审计。** startup check 通过后，长任务中服务失联是否有 heartbeat/recheck。
- **遗漏 5：多 run 资源隔离未审计。** MCP pool、browser context、临时目录和日志是否按 run 隔离未证明。

### 9. Security / SSRF / Prompt Injection / Secrets / Supply Chain

- **遗漏 1：DNS rebinding 未审计。** host 初次解析为公网、连接时解析为私网的情况仍可能绕过简单校验。
- **遗漏 2：跨协议与非 HTTP scheme 未审计。** `file:`, `data:`, `ftp:` 或自定义 scheme 是否被拒绝需显式测试。
- **遗漏 3：抓取内容中的工具调用指令未审计。** untrusted page 可能诱导模型调用 fetch、shell 或泄露配置；验证：使用无害 injection fixture。
- **遗漏 4：日志和持久化的二次泄露未审计。** page content、headers、raw plugin error 在 debug、IndexedDB、report 三处的脱敏边界需逐层检查。
- **遗漏 5：依赖和 CDN 完整性未审计。** lockfile、动态 CDN、MCP manifest 的版本漂移和 checksum/签名策略未形成审计记录。

### 10. Tests / Packaging / Deployment / Audit Process

- **遗漏 1：测试命令与 package scripts 的一致性未审计。** 不能只看单个失败命令，需记录 `package.json`、lockfile 和实际入口。
- **遗漏 2：测试 fixture 是否覆盖真实 transport shape 未审计。** mock 成功响应可能缺少 status、headers、attempt facts，导致假绿。
- **遗漏 3：目标仓库与代理 worktree 的 commit 关联未审计。** 每个修复声明都需映射到目标路径、commit、dirty snapshot 和测试结果。
- **遗漏 4：审计记录自身的 schema 演进未审计。** Markdown 与 JSON 的字段、数量、ID、状态是否自动比对尚未形成命令。
- **遗漏 5：复验失败的保留策略未审计。** 失败日志、退出码、环境摘要和 artifact 路径是否 append-only 保存需补充。

#### F-139 复验闭环：SSRF/private-IP 防护（needs-verification）

- **问题**：crawl URL 进入 local fetch、browser fetch 或 MCP worker 前缺少统一 SSRF/private-IP 边界，且仅校验初始 URL 可能无法阻止重定向进入内部地址。
- **最小复现**：对 `http://127.0.0.1:8080/internal` 的 MCP fetch 请求断言 worker 不被调用；对 BrowserAdapter 返回 `http://127.0.0.1:8080/internal` 的 `finalUrl` 断言拒绝。修复前分别表现为 worker 被调用或 browser fallback 吞掉 unsafe final URL。
- **修复**：新增共享 `src/fetch-fusion/network-safety.ts`，仅允许 HTTP(S)，拒绝 localhost、`.localhost`、`.local`、保留 IPv4 与 IPv6；local fetch 校验初始 URL及 WebFetch/raw response 最终 URL，browser fetch 校验入口、Playwright `page.url()` 和 adapter `finalUrl`，MCP adapter 在 `callTool` 前校验，vendored worker 校验初始与 `response.url` 最终 URL。
- **验证证据**：network-safety、browser-fetch、MCP adapter focused 回归通过（MCP/network 12/12）；vendored worker 独立 `node --test test/ssrf.test.js` 2/2 通过，覆盖 public-to-private redirect 与 unsafe initial target，且 unsafe-network error 不再被 worker 转成普通结构化失败；full suite 225/225；`tsc -p tsconfig.json --noEmit`、`git diff --check`、`pnpm build` 均通过。覆盖 literal loopback/private/link-local/ULA、非 HTTP(S)、localhost、browser unsafe final URL 和 MCP pre-call rejection。
- **剩余边界**：vendored worker redirect-to-private 集成测试已补齐；普通 hostname 解析到私网、DNS rebinding，以及 Playwright 每个 route request 的独立目标校验仍未覆盖。因此 F-139 保持 `needs-verification`，不宣称全部 SSRF 场景 fixed。


每个 section 已补列 5 个不重复的判断疏漏，共 50 项复审检查点；这些检查点不是额外声称的 50 条已确认 bug。后续应逐项转为独立 finding 或关闭为“无复现”，并将命令、退出码、目标 commit 和证据 artifact 写回 `findings-inventory.json`。当前 inventory 的 167 条记录仍保持原状态，不因这 50 项检查点自动升级为 confirmed。

## F-016 复验闭环：evidence assessment 持久化

- **问题**：模型返回的 `decision.evidenceAssessments` 已在协议层解析，但此前没有写回对应的 `FetchedPageRecord`；因此最终 `deriveEarlyAccessItems` 读取不到模型拥有的 `qualityCategory`，有效 evidence 可能在 report boundary 被过滤。
- **最小复现**：`__tests__/app/policy-loop-behavior.test.ts` 先以第二轮 assessment 返回 `SILVER_STANDARD` 与理由，初始失败为 `undefined !== 'SILVER_STANDARD'`；该失败确认缺口来自生产回写行为而非测试语法。
- **修复**：`src/app/run-policy-task.ts:27` 新增 URL 规范化及 assessment-to-page 合并，仅按模型提供的 assessment 匹配 `requestedUrl` 或 `finalUrl`，写入 `qualityCategory` 与 `validationReason`；runtime 不自行推断质量，也不恢复 target gate。
- **验证证据**：focused policy-loop 文件通过 15/15；full suite 通过 207/207；`tsc -p tsconfig.json --noEmit`、`git diff --check`、`pnpm build` 均通过。
- **状态**：`F-016` → `fixed`，confidence `confirmed`。仍需在后续 live-audit fixture 中持续验证 assessment 到最终 report artifact 的传播，但当前 loop/state boundary 已有直接回归证据。



### F-022 复验闭环：模型请求超时配置 finite/range 校验

- **问题**：`LIVE_AUDIT_MODEL_TIMEOUT_MS` 曾通过 `Number()` 直接进入 `requestTimeoutMs`，`NaN`/`Infinity` 或负数可能穿透配置边界。
- **最小复现**：新增 `nanoclaw-bridge` 回归，以 `LIVE_AUDIT_MODEL_TIMEOUT_MS=NaN` 断言配置解析应拒绝；修复前 focused 测试因 `Missing expected exception` 失败。
- **修复**：在 `src/runtime/nanoclaw-bridge.ts` 增加 `parseOptionalTimeout`，仅允许未设置或非负 finite 数值；保留合法 timeout 与空值兼容。
- **验证**：focused 16/16；full suite、TypeScript no-emit、`git diff --check`、`pnpm build` 均通过。
- **状态**：fixed。
